# Usage of a packaged app

A command-line call to the packaged app `./app a b` is equivalent to `node app.js a b`.

See also [Snapshot filesystem](/guide/snapshot-fs) for the values of `__filename`, `__dirname`, `process.cwd()`, and friends at runtime.
